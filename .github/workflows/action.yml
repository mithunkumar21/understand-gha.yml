name: the anatomy of a github action

on: [push]

jobs:
   action:
      runs-on: ubuntu-latest
      steps:
         - name: checkout repo
           uses: actions/checkout@v1

         - name: npm install and build webpack
           run: |
              npm install 
              npm run build 